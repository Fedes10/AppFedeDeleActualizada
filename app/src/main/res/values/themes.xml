<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DeleFede" parent="Theme.Material3.Light.NoActionBar">
        <!-- Tema principal blanco y naranja suave -->
        <item name="colorPrimary">@color/orange_soft</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/orange_accent</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorOnBackground">@color/text_primary</item>
        <item name="colorOutline">@color/light_gray</item>
        <!-- Tipografía moderna -->
        <item name="fontFamily">sans-serif</item>
    </style>

    <!-- Tema principal -->
    <style name="Theme.DeleFede" parent="Base.Theme.DeleFede" />
</resources>